import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wanderingraccooncheer',
  appName: 'wandering-raccoon-cheer',
  webDir: 'dist'
};

export default config;
